import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({
    pageNotFound: {
        background: "#0C374A",
        height: "100vh",
        textAlign: "center",
        margin: "0 auto",
        color: "#fff",
    },
}));

export default useStyles;